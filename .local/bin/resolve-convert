#!/bin/bash

if [ -z "$1" ]; then
    echo "Usage: resolve-convert <file>"
    exit 1
fi

INPUT="$1"
BASENAME="${INPUT%.*}"
OUTPUT="${BASENAME}-resolved.mov"

ffmpeg -i "$INPUT" -c:v prores_ks -profile:v 0 -c:a pcm_s16le "$OUTPUT"

echo "Done: $OUTPUT"
