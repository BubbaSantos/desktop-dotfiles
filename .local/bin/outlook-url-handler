#!/bin/bash

url="$1"

# If it's an Outlook-related URL, open in the Outlook app
if [[ "$url" =~ outlook\.(live|office|com) ]] || [[ "$url" =~ ^mailto: ]]; then
    /opt/outlook-for-linux/outlook-for-linux "$url" &
else
    # For other URLs, use default browser
    xdg-open "$url"
fi
